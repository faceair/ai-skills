interface:
  display_name: "Guance RUM Instrumentation"
  short_description: "Plan and instrument every Guance RUM app type"
  default_prompt: "Use $rum-instrument to detect every deployable RUM target in this repository, infer a safe Guance RUM profile from minimal appId and receiver variables, create an approval-gated plan, implement only an approved revision, and validate the resulting integration."
